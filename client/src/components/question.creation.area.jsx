import React, { Component } from "react";
import { Button, Navbar, Container, Row, Col } from "reactstrap";

class QuestionCreationArea extends Component {
  state = {};
  render() {
    return (
      <React.Fragment>
        <Button color="primary">Create a New Question</Button>
      </React.Fragment>
    );
  }
}

export default QuestionCreationArea;
